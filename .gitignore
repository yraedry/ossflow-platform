__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
.env
.env.local
*.egg-info/
build/
dist/
.coverage
htmlcov/
.idea/
.vscode/
docs/superpowers/
node_modules/

# Runtime settings (contain real API keys — NEVER commit)
**/_settings.json

# Runtime pytest state
**/.pytest_config/

# Voice samples (large binaries, not source-of-truth)
ossflow-dubbing/voices/
ossflow-dubbing/model_voices/*.wav

# Local TTS model weights (Fish Audio S2-Pro GGUF, ~4.5 GB).
# Bind-mounted into ossflow-dubbing at /models/s2pro:ro.
models/
