# Dubbing-generator deps.
# TTS: S2-Pro (Fish Audio, voice cloning local, backend CUDA) — único motor
# soportado tras T22.5 (eliminados ElevenLabs/Piper/Kokoro).
# XTTS-v2 eliminado previamente — usaba coqui-tts + transformers + ~8-10GB.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Audio stack — demucs (GPU) separa voz/fondo para el Flujo B v5.
demucs
pydub
soundfile>=0.12
# librosa powers the QA boundary report (spectral centroid + pyin F0).
# Soft dep of the qa module — if missing the sidecar falls back to MOS-only.
librosa>=0.10.1

# Cliente HTTP para SynthesizerS2Pro + S2ProServerManager health probe
# contra el server s2.cpp embebido en el contenedor.
httpx>=0.27

# Test-only
pytest>=7.4
pytest-asyncio>=0.23
